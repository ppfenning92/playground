/** @type {import("prettier").Config} */
const config = {
  printWidth: 80,
  bracketSpacing: true,
  jsonRecursiveSort: true,
  singleQuote: true,
  tabWidth: 2,

  trailingComma: 'all',
};

export default config;
